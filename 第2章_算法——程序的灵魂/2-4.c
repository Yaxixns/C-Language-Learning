/*
 * 习题4
 * 用传统流程图表示求解以下问题的算法。
 * （1）有两个瓶子 A 和 B，分别盛放醋和酱油，要求将它们互换（即 A 瓶原来盛醋，
 *      现改盛酱油，B 瓶则相反）。
 * （2）依次将 10 个数输入，要求输出其中最大的数。
 * （3）有 3 个数 a,b,c，要求按大小顺序把它们输出。
 * （4）求 1+2+3+...+100。
 * （5）判断一个数 n 能否同时被 3 和 5 整除。
 * （6）将 100~200 之间的素数输出。
 * （7）求两个数 m 和 n 的最大公约数。
 * （8）求方程式 ax^2+bx+c=0 的根。分别考虑：
 *     ① 有两个不等的实根；
 *     ② 有两个相等的实根。
*/

/*
 * 参考答案（文字描述流程图，考试须画图）：
 *
 * （1）交换 A、B（引入中间变量 C）：
 *     开始 → C=A → A=B → B=C → 结束
 *
 * （2）10 个数求最大值：
 *     开始 → 输入第1个数→max → i=2
 *         → [i<=10?]
 *              是→输入x → [x>max?]→是→max=x → i++ → 回到判断
 *              否→输出max → 结束
 *
 * （3）3 个数排序：
 *     开始 → 输入a,b,c
 *         → [a>b?] ─是→ 交换a,b
 *         → [a>c?] ─是→ 交换a,c
 *         → [b>c?] ─是→ 交换b,c
 *         → 输出a,b,c → 结束
 *
 * （4）1+2+...+100：
 *     开始 → sum=0,i=1
 *         → [i<=100?]
 *              是→ sum=sum+i → i=i+1 → 回到判断
 *              否→ 输出sum → 结束
 *
 * （5）判断 n 同时被 3 和 5 整除：
 *     开始 → 输入n
 *         → [n%3==0 且 n%5==0?]
 *              是→ 输出"能整除"
 *              否→ 输出"不能整除"
 *         → 结束
 *
 * （6）100~200 间素数：
 *     开始 → n=100
 *         → [n<=200?]
 *              否→结束
 *              是→ flag=1, i=2
 *                 → [i<=sqrt(n)?]
 *                      否→ [flag==1?]→是→输出n → n++ → 回到外层
 *                      是→ [n%i==0?]→是→flag=0,break → [flag==1?]...
 *                                      →否→i++ → 回到内层判断
 *
 * （7）最大公约数（辗转相除）：
 *     开始 → 输入m,n
 *         → [n!=0?]
 *              是→ r=m%n → m=n → n=r → 回到判断
 *              否→ 输出m → 结束
 *
 * （8）求二次方程根：
 *     开始 → 输入a,b,c → delta=b²-4ac
 *         → [delta>0?]─是→ x1=(-b+√delta)/2a, x2=(-b-√delta)/2a → 输出
 *         → [delta==0?]─是→ x=-b/2a → 输出重根
 *         → 否则输出"无实根"
 *         → 结束
 */
