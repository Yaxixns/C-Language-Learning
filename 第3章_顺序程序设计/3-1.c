/*
 * 习题1
 * 假如我国国民生产总值的年增长率为 7%，计算 10 年后我国国民生产总值
 * 与现在相比增长多少百分比。计算公式为：
 *     p = (1 + r)^n
 * r 为年增长率，n 为年数，p 为与现在相比的倍数（即增长了 p-1 的百分比）。
 */

#include <stdio.h>

int main()
{
    float p = 1.0;
    int i;
    for (i = 0; i < 10; i++)
    {
        p = p * (1 + 0.07);
    }
    printf("%.2f%%\n", (p - 1) * 100);
    getchar();
    return 0;
}
/* ────────────────────────────────────────
 * 📝 批改评语：✅ 正确！10/10
 *
 * ① 循环10次计算 (1.07)^10，结果正确。
 * ② (p-1)*100 得到增长百分比，逻辑对。
 * ③ printf 用 %% 输出百分号，用法标准。
 * ④ printf 用 %.2f 控制两位小数，满足要求。
 *
 * 💡 拓展：也可以用 pow(1.07, 10) 一行搞定，
 *    但你用循环手算幂次，体现对循环的理解，也很好。
 * ──────────────────────────────────────── */
