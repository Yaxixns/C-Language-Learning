/*
 * 习题6
 * 求 1! + 2! + 3! + 4! + ... + 20!（即 1 到 20 的阶乘之和）。
 */

#include <stdio.h>
int main()
{
    int i;
    unsigned long long int temp = 1, sum = 0;
    for (i = 1; i <= 20; i++)
    {
        temp *= i;
        sum += temp;
    }

    printf("%llu\n", sum);
    getchar();
    return 0;
}

/*
 * === 复习笔记：本次犯的错误 ===
 *
 * 1. 数据类型太小导致溢出
 *    - 错误：用 int（32 位，最大值 ~21 亿）存储阶乘和
 *    - 原因：13! ≈ 62 亿，已超出 int 范围，结果完全错误
 *    - 修正：用 unsigned long long（64 位，最大值 ~1.8×10¹⁹），20! ≈ 2.4×10¹⁸ 装得下
 *
 * 2. printf 格式符用错
 *    - 错误：用 %d 打印结果
 *    - 修正：unsigned long long 对应 %llu
 *
 * 3. 变量重复声明
 *    - 错误：第 9 行声明了 int i，for 循环里又 int i = 1
 *    - 修正：删掉外层，只在 for 里声明（或反过来）
 */
/* ────────────────────────────────────────
 * 📝 批改评语：✅ 正确 + 复习笔记很赞！10/10
 *
 * ① 用 unsigned long long 避免溢出 — 命中考点
 * ② temp *= i 递推阶乘，O(n) 高效
 * ③ %llu 格式符匹配正确
 * ④ 自写复习笔记总结了 3 个错误，学习态度满分
 * ──────────────────────────────────────── */
