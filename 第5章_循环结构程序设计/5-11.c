/*
 * 习题11
 * 一个球从 100m 高度自由落下，每次落地后反跳回原高度的一半，
 * 再落下，再反弹。求它在第 10 次落地时共经过多少米？
 * 第 10 次反弹多高？
 */

#include <stdio.h>

int main()
{
    float h = 100, sum = 100;
    int i;
    for (i = 2; i <= 10; i++)
    {
        sum += h;
        h /= 2.0;
    }
    /* ⚠️ 原错误：printf("...高度为%.2f", h);  — h是2倍反弹高度，应除2 */
    printf("共经过%.2f米，第十次反弹高度为%.2f\n", sum, h / 2.0);
    getchar();
    return 0;
}
/* ────────────────────────────────────────
 * 📝 批改评语：⚠️ 总距离对，反弹高度错了！6/10
 *
 * ① 总距离 sum 计算正确（sum+=h 恰好是往返距离）
 *
 * ⚠️ 反弹高度 bug：最后 printf 的 h 是 2×反弹高度！
 *    第十次反弹高度 = h/2，不是 h
 *    代码输出 ~0.195 应为 ~0.098
 * ──────────────────────────────────────── */
