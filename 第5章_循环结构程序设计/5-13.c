/*
 * 习题13
 * 用迭代法求 x = sqrt(a)。求平方根的迭代公式为：
 *     x_{n+1} = (x_n + a/x_n) / 2
 * 要求前后两次求出的 x 的差的绝对值小于 10^(-5)。
 */

#include <stdio.h>
#include <math.h>

int main()
{
    int a;
    scanf("%d", &a);
    double x0, x1 = a;
    do
    {
        x0 = x1;
        x1 = (x0 + a / x0) / 2.0;
    } while (fabs(x1 - x0) > 1e-5);
    printf("%.3f\n", x1);
    getchar();
    return 0;
}

/*
 * === 复习笔记：本次犯的错误 ===
 *
 * 1. 用 while + 假初值骗循环，只迭代了一轮
 *    - 错误：设 x_nect = 0 只为进入 while，第一轮算完 x 和 x_nect 就相等了，循环立刻退出
 *    - 修正：用 do-while，先算再判断，用 x0 存旧值、x1 存新值，比较两者之差
 *
 * 2. 整数取绝对值用 abs()，浮点数要用 fabs()
 *    - 需要 #include <math.h>
 *
 * 3. 循环体内忘记把新值赋回旧变量
 *    - while 写法中 x = x_nect 这句不能漏，否则每轮都在算同一个值
 *
 * 4. 迭代初值取 a 即可（牛顿法收敛很快）
 */

/* ────────────────────────────────────────
 * 📝 批改评语：✅ 正确！复习笔记很赞 10/10
 *
 * ① 牛顿迭代法 do-while 实现正确
 * ② fabs() 判断收敛条件 — 正确（浮点绝对值用 fabs 不是 abs）
 * ③ 复习笔记总结了 4 个自己踩的坑，印象深刻！
 * ──────────────────────────────────────── */
