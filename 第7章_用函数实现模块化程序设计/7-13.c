/*
 * 习题13
 * 用递归方法求 n 阶勒让德多项式的值。递归公式为：
 *   p_0(x) = 1
 *   p_1(x) = x
 *   p_n(x) = ((2n-1) * x * p_{n-1}(x) - (n-1) * p_{n-2}(x)) / n   (n >= 1)
*/

#include <stdio.h>

int main()
{

    
    getchar();
    return 0;
}

