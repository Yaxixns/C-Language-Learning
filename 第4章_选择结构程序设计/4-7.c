/*
 * 习题7
 * 有一函数：
 *         /  -1  (x < 0)
 *   y =  |   0  (x = 0)
 *         \   1  (x > 0)
 *
 * 有人编写了以下两个程序，请分析它们是否能实现题目要求。
 * 不要急于上机运行程序，先分析两个程序的逻辑，画出它们的流程图，
 * 分析它们的运行情况，然后上机运行程序，观察和分析结果。
 *
 * (1)
 *   #include <stdio.h>
 *   int main()
 *   {
 *       int x, y;
 *       printf("enter x:");
 *       scanf("%d", &x);
 *       y = -1;
 *       if (x != 0)
 *           if (x > 0)
 *               y = 1;
 *           else
 *               y = 0;
 *       printf("x=%d,y=%d\n", x, y);
 *       return 0;
 *   }
 *
 * (2)
 *   #include <stdio.h>
 *   int main()
 *   {
 *       int x, y;
 *       printf("enter x:");
 *       scanf("%d", &x);
 *       y = 0;
 *       if (x >= 0)
 *           if (x > 0) y = 1;
 *           else y = -1;
 *       printf("x=%d,y=%d\n", x, y);
 *       return 0;
 *   }
 */

/*
(1)
不对 应该把y=-1改成y=0
(2)
不对 应该把if (x >= 0)改成if (x != 0)
*/
/* ────────────────────────────────────────
 * 📝 批改评语：⚠️ 分析有对有错！7/10
 *
 * 你的判断：
 *   (1) 不对 ✓
 *   (2) 不对 ✓
 * 两个都判断对了，程序确实都有bug。
 *
 * 但你的修改建议：
 *   (1) "把y=-1改成y=0" — ❌ 这样改还是错的！
 *   (2) "把if(x>=0)改成if(x!=0)" — ✅ 正确！
 *
 * 详细分析：
 *
 * 【程序(1)】
 *   y = -1;
 *   if (x != 0)
 *       if (x > 0) y = 1;
 *       else       y = 0;
 *   // else 配对的是 if(x>0)，不是 if(x!=0)！
 *
 *   x<0：进入外层if → x>0为假 → else执行y=0。输出y=0，但应该是-1 ✗
 *   x=0：外层if不进入。y保持-1。输出y=-1，但应该是0 ✗
 *   x>0：进入外层if → x>0为真 → y=1。输出y=1 ✓
 *
 *   正确的改法（不是改y=-1）：
 *   在 if(x!=0) 后面加大括号，强制 else 与 if(x!=0) 配对：
 *     if (x != 0) {
 *         if (x > 0) y = 1;
 *     } else y = 0;
 *
 * 【程序(2)】
 *   y = 0;
 *   if (x >= 0)
 *       if (x > 0) y = 1;
 *       else       y = -1;
 *
 *   x<0：外层if不进入。y保持0，应该是-1 ✗
 *   x=0：x>0为假 → else y=-1，应该是0 ✗
 *   x>0：y=1 ✓
 *
 *   你建议改 if(x!=0) 后：
 *   x<0：进入外层if → x>0为假 → else y=-1。y=-1 ✓
 *   x=0：外层if不进入。y=0 ✓
 *   x>0：y=1 ✓
 *   全部正确！
 *
 * 📌 经典考点：else 总是与前面最近的未配对 if 配对！
 *    缩进会骗人，必须看大括号。
 * ──────────────────────────────────────── */
