/*
 * 习题2
 * C 语言中如何表示"真"和"假"？系统如何判断一个量的"真"和"假"？
*/

/*
 * 参考答案：
 *
 * （1）C语言如何表示真和假？
 *     用数值来表示：0 表示"假"，非0（即任意非零值）表示"真"。
 *     ⚠️ 不是"1表示真"！2、-1、3.14、'a' 都被视为真。
 *     关系表达式和逻辑表达式的计算结果为 1（真）或 0（假）。
 *     例如：5>3 的结果是 1，5<3 的结果是 0。
 *
 * （2）系统如何判断一个量的真假？
 *     在需要逻辑判断的场合（if、while、for的条件），
 *     系统以 0 为假，任何非0 值为真。
 *     例如：
 *       if (5)    → 真（5≠0）
 *       if (0)    → 假
 *       if (-1)   → 真
 *       if ('\0') → 假（'\0'的ASCII码为0）
 *
 * 📌 你写的"1表示真 0表示假"只对了一半——
 *    逻辑表达式的结果确实是1或0，但判断真假时是非0即真！
 */
