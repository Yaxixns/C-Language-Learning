/*
 * 习题9
 * 给一个不多于 5 位的正整数，要求：
 * ① 求出它是几位数；
 * ② 分别输出每一位数字；
 * ③ 按逆序输出各位数字，例如原数为 321，应输出 123。
 */

#include <stdio.h>

int main()
{
    int num, count = 0;
    int digits[5];          /* 最多5位数，存放每一位（个位→高位） */

    printf("请输入一个不多于5位的正整数：");
    scanf("%d", &num);

    /* ① 拆解每一位并统计位数 */
    int temp = num;
    while (temp > 0) {
        digits[count] = temp % 10;   /* 取最后一位 */
        temp /= 10;
        count++;
    }

    /* ① 输出位数 */
    printf("① 它是 %d 位数\n", count);

    /* ② 顺序输出每一位数字（digits是逆序存的，需倒着遍历） */
    printf("② 每一位数字：");
    for (int i = count - 1; i >= 0; i--) {
        printf("%d", digits[i]);
        if (i > 0) putchar(' ');
    }
    putchar('\n');

    /* ③ 逆序输出各位数字 */
    printf("③ 逆序输出：");
    for (int i = 0; i < count; i++) {
        printf("%d", digits[i]);
    }
    putchar('\n');

    return 0;
}
/* ────────────────────────────────────────
 * 📝 批改评语：✅ 逻辑清晰！10/10
 *
 * ① 位数统计：while+取余逐位拆分，count 计数 — 正确。
 * ② 顺序输出：从 count-1 倒序遍历 digits 数组 — 正确。
 * ③ 逆序输出：从 0 正序遍历 — 正确。
 * ④ 数组大小 digits[5] 恰好容纳5位数。
 *
 * 💡 小建议：
 *   - 如果输入0会怎样？while(temp>0)不执行，count=0。
 *     但题目说"正整数"，0不在范围，无妨。
 *   - for(int i=...) 中声明变量是 C99 语法，有些老编译器
 *     可能报错，考试建议在函数开头统一声明 int i。
 *
 * 整体逻辑流畅，注释清晰，很好的代码风格！
 * ──────────────────────────────────────── */
