/*
 * 习题3
 * 写出下面各逻辑表达式的值。设 a=3, b=4, c=5。
 * （1）a + b > c && b == c
 * （2）a || b + c && b - c
 * （3）!(a > b) && !c || 1
 * （4）!(x = a) && !(y = b) && 0
 * （5）!(a + b) + c - 1 && b + c / 2
*/

/*
 * 你答的：0、1、1、0、1 — 全部正确！✅
 *
 * 详细计算过程（考试时要写）：
 *
 * （1）a + b > c && b == c
 *     = 3+4 > 5 && 4 == 5
 *     = 7 > 5 && 4 == 5
 *     = 1 && 0
 *     = 0
 *
 * （2）a || b + c && b - c
 *     优先级：算术 > && > ||
 *     = 3 || (4+5) && (4-5)
 *     = 3 || 9 && (-1)
 *     = 3 || (9 && -1)    // 9非0→真, -1非0→真, 9&&-1=1
 *     = 3 || 1            // 3非0→真
 *     = 1
 *
 * （3）!(a > b) && !c || 1
 *     优先级：! > && > ||
 *     = !(0) && !5 || 1   // a>b为假=0
 *     = 1 && 0 || 1       // !5=0（5非0为真，取反=0）
 *     = 0 || 1
 *     = 1
 *     ⚠️ 注意：|| 后面有1，无论前面结果如何，整体必为1（短路）
 *
 * （4）!(x = a) && !(y = b) && 0
 *     = !(x=3) && !(y=4) && 0  // 赋值表达式的结果是赋值后的值
 *     = !3 && !4 && 0           // 3非0→!3=0
 *     = 0 && ...                // &&短路：左边已为0
 *     = 0
 *
 * （5）!(a + b) + c - 1 && b + c / 2
 *     优先级：! > 算术 > &&
 *     = !(7) + 5 - 1 && 4 + 2   // c/2=5/2=2（整数除法）
 *     = 0 + 5 - 1 && 6
 *     = 4 && 6                   // 4非0→真, 6非0→真
 *     = 1
 *
 * 💡 关键考点：&& 和 || 的短路特性、运算符优先级、整数除法。
 */
