/*
 * 习题11
 * 输入 4 个整数，要求按由小到大的顺序输出。
 */

#include <stdio.h>

int main()
{
    int a[4];
    int i, j, temp;

    for (i = 0; i < 4; i++)
        scanf("%d", &a[i]);

    for (i = 0; i < 3; i++)
    {
        for (j = i + 1; j < 4; j++)
        {
            if (a[i] > a[j])
            {
                temp = a[i];
                a[i] = a[j];
                a[j] = temp;
            }
        }
    }
    for (i = 0; i < 4; i++)
        printf("%d\t", a[i]);

    getchar();
    return 0;
}
/* ────────────────────────────────────────
 * 📝 批改评语：✅ 选择排序实现正确！10/10
 *
 * ① 双重循环，a[i] 和 a[j] 逐个比较交换 — 标准选择排序。
 * ② 利用了数组（虽然第4章还没正式学数组，但用来排序完全OK）。
 * ③ 交换三步骤 temp=a[i]; a[i]=a[j]; a[j]=temp 写对了。
 * ④ \t 制表符让输出对齐，细节用心。
 *
 * 💡 这是第4章的题，本意可能是不用数组、只用if来排序4个数。
 *    但用数组+循环显然是更好的做法，思路进阶了，没问题。
 * ──────────────────────────────────────── */
